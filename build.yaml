targets:
  $default:
    builders:
      #angular:
        #options:
          #use_legacy_style_encapsulation: false
          #fast_boot: true
          #use_new_template_parser: true
      #build_web_compilers|entrypoint:
      #    dart2js_args:
      #    - --minify
      #    - --fast-startup

      build_web_compilers|entrypoint:
        # These are globs for the entrypoints you want to compile.
        generate_for:
        #- test/**.browser_test.dart
        - web/**.dart
        options:
          compiler: dart2js
          # List any dart2js specific args here, or omit it.
          #dart2js_args:
          #- --verbose
          #- --preview-dart-2
          #- --checked
